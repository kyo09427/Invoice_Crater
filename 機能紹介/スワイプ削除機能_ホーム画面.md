# 精算書削除機能 - 実装ガイド（ホーム画面）

## 📱 機能概要

ホーム画面（精算書一覧）で、**左にスワイプ**することで精算書全体を削除できる機能です。

### 主な特徴

✅ **直感的な操作**: 左スワイプで削除アクションを実行  
✅ **重要な警告**: すべての明細データが失われることを明示  
✅ **詳細な確認**: 削除対象の精算書情報を表示  
✅ **誤操作防止**: 確認ダイアログによる二重チェック  
✅ **視覚的フィードバック**: 赤い背景と削除アイコン  
✅ **エラーハンドリング**: 削除失敗時の適切な処理

## 🎨 UIデザイン

### スワイプ時の背景

```dart
background: Container(
  decoration: BoxDecoration(
    color: Colors.red,
    borderRadius: BorderRadius.circular(16), // カードの角丸に合わせる
  ),
  alignment: Alignment.centerRight,
  padding: const EdgeInsets.only(right: 24),
  child: const Column(
    mainAxisAlignment: MainAxisAlignment.center,
    children: [
      Icon(
        Icons.delete_outline,
        color: Colors.white,
        size: 36, // 明細より大きめ
      ),
      SizedBox(height: 4),
      Text(
        '削除',
        style: TextStyle(
          color: Colors.white,
          fontWeight: FontWeight.bold,
          fontSize: 14,
        ),
      ),
    ],
  ),
)
```

**デザインポイント**:
- カードの`borderRadius`に合わせて背景も角丸
- アイコンサイズを36pxに拡大（明細の32pxより大きい）
- 精算書削除は重要な操作なので視認性を強化

### 確認ダイアログ

```dart
AlertDialog(
  title: const Text('精算書を削除'),
  content: Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      // 警告メッセージ
      const Text('この精算書を削除してもよろしいですか？'),
      const Text(
        '削除すると、すべての明細データも失われます。',
        style: TextStyle(
          color: Colors.red,
          fontSize: 12,
          fontWeight: FontWeight.bold,
        ),
      ),
      
      // 精算書情報カード
      Container(
        padding: const EdgeInsets.all(12),
        decoration: BoxDecoration(
          color: Colors.grey[100],
          borderRadius: BorderRadius.circular(8),
        ),
        child: Column(
          children: [
            // タイトル
            Row(
              children: [
                Icon(Icons.receipt_long),
                Text(sheet.title, style: TextStyle(fontWeight: FontWeight.bold)),
              ],
            ),
            
            // 作成日と明細数
            Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                Text('作成日: ${FormattingUtils.formatDate(sheet.createdAt)}'),
                Text('${sheet.items.length}件'),
              ],
            ),
            
            // 合計金額（赤字で強調）
            Text(
              FormattingUtils.formatCurrency(sheet.totalAmount),
              style: TextStyle(
                fontWeight: FontWeight.bold,
                fontSize: 18,
                color: Colors.red,
              ),
            ),
          ],
        ),
      ),
    ],
  ),
  actions: [
    TextButton(
      onPressed: () => Navigator.pop(context, false),
      child: const Text('キャンセル'),
    ),
    FilledButton(
      onPressed: () => Navigator.pop(context, true),
      style: FilledButton.styleFrom(backgroundColor: Colors.red),
      child: const Text('削除'),
    ),
  ],
)
```

**ダイアログの特徴**:
1. **警告の強調**: 明細データも失われることを赤字で明示
2. **詳細情報**: タイトル・作成日・明細数・合計金額を表示
3. **視覚的階層**: アイコン付きで分かりやすく
4. **金額の強調**: 赤字・太字・大きめフォント

## 🔧 実装の詳細

### 1. Dismissibleの設定

```dart
Dismissible(
  key: Key(sheet.id), // ユニークキー
  direction: DismissDirection.endToStart, // 右から左へ
  
  // 削除確認
  confirmDismiss: (direction) async {
    return await _confirmDeleteSheet(context, sheet);
  },
  
  // 削除実行
  onDismissed: (direction) async {
    await _onDismissed(context, ref, sheet);
  },
  
  background: /* 上記の背景UI */,
  
  child: /* 精算書カード */,
)
```

### 2. 削除確認メソッド

```dart
Future<bool?> _confirmDeleteSheet(
  BuildContext context,
  ExpenseSheet sheet,
) async {
  return showDialog<bool>(
    context: context,
    builder: (BuildContext dialogContext) {
      return AlertDialog(
        title: const Text('精算書を削除'),
        content: Column(
          mainAxisSize: MainAxisSize.min,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            // 基本警告
            const Text('この精算書を削除してもよろしいですか？'),
            const SizedBox(height: 8),
            
            // 重要な警告（赤字）
            const Text(
              '削除すると、すべての明細データも失われます。',
              style: TextStyle(
                color: Colors.red,
                fontSize: 12,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 16),
            
            // 精算書情報表示
            Container(
              padding: const EdgeInsets.all(12),
              decoration: BoxDecoration(
                color: Colors.grey[100],
                borderRadius: BorderRadius.circular(8),
                border: Border.all(color: Colors.grey[300]!),
              ),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  // タイトル
                  Row(
                    children: [
                      Icon(Icons.receipt_long, size: 20),
                      const SizedBox(width: 8),
                      Expanded(
                        child: Text(
                          sheet.title,
                          style: const TextStyle(
                            fontWeight: FontWeight.bold,
                            fontSize: 16,
                          ),
                        ),
                      ),
                    ],
                  ),
                  const SizedBox(height: 8),
                  
                  // メタ情報
                  Row(
                    mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      Text(
                        '作成日: ${FormattingUtils.formatDate(sheet.createdAt)}',
                        style: TextStyle(fontSize: 12, color: Colors.grey[600]),
                      ),
                      Text(
                        '${sheet.items.length}件',
                        style: TextStyle(fontSize: 12, color: Colors.grey[600]),
                      ),
                    ],
                  ),
                  const SizedBox(height: 4),
                  
                  // 合計金額
                  Text(
                    FormattingUtils.formatCurrency(sheet.totalAmount),
                    style: const TextStyle(
                      fontWeight: FontWeight.bold,
                      fontSize: 18,
                      color: Colors.red,
                    ),
                  ),
                ],
              ),
            ),
          ],
        ),
        actions: [
          TextButton(
            onPressed: () => Navigator.of(dialogContext).pop(false),
            child: const Text('キャンセル'),
          ),
          FilledButton(
            onPressed: () => Navigator.of(dialogContext).pop(true),
            style: FilledButton.styleFrom(backgroundColor: Colors.red),
            child: const Text('削除'),
          ),
        ],
      );
    },
  );
}
```

### 3. 削除実行メソッド

```dart
Future<void> _onDismissed(
  BuildContext context,
  WidgetRef ref,
  ExpenseSheet sheet,
) async {
  try {
    // データベースから削除
    await ref.read(expenseSheetListProvider.notifier).deleteSheet(sheet.id);

    // 成功フィードバック
    if (context.mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text('「${sheet.title}」を削除しました'),
          duration: const Duration(seconds: 3),
          action: SnackBarAction(
            label: '元に戻す',
            onPressed: () {
              // TODO: v1.2で実装予定
            },
          ),
        ),
      );
    }
  } catch (e) {
    // エラーハンドリング
    if (context.mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(
          content: Text('削除に失敗しました: $e'),
          backgroundColor: Colors.red,
          duration: const Duration(seconds: 4),
        ),
      );
    }
  }
}
```

## 📊 明細削除との違い

| 項目 | 明細削除 | 精算書削除 |
|-----|---------|-----------|
| **対象** | 1つの明細 | 精算書全体 + 全明細 |
| **重要度** | 中 | 高 |
| **警告** | 基本的な確認 | 「すべての明細も削除」と強調 |
| **アイコンサイズ** | 32px | 36px |
| **確認情報** | 支払い先・用途・金額 | タイトル・日付・明細数・合計金額 |
| **背景色** | 赤 | 赤（同じ） |
| **元に戻す** | v1.2予定 | v1.2予定 |

## ⚠️ 重要な考慮事項

### 1. データの永続性

```dart
// 削除は即座に実行され、現在は元に戻せない
await ref.read(expenseSheetListProvider.notifier).deleteSheet(sheet.id);
```

**注意点**:
- Hiveから完全に削除される
- 明細データもすべて失われる
- 現在はバックアップ機能がない

### 2. カスケード削除

```dart
// ExpenseSheet削除時、items（明細リスト）も自動的に削除される
// Hiveのモデル構造上、ExpenseSheetが削除されれば
// そのitemsフィールドも一緒に削除される
```

### 3. エラーリカバリ

```dart
try {
  await deleteSheet(sheet.id);
} catch (e) {
  // エラー時は精算書が削除されずにそのまま残る
  // UIには表示され続ける（Dismissibleがキャンセルされる）
}
```

## 🚀 今後の拡張予定

### v1.2で実装予定

1. **元に戻す機能**
   ```dart
   class DeletedSheetCache {
     final Map<String, ExpenseSheet> _cache = {};
     final Map<String, DateTime> _deleteTime = {};
     
     void cache(ExpenseSheet sheet) {
       _cache[sheet.id] = sheet;
       _deleteTime[sheet.id] = DateTime.now();
       
       // 30秒後に自動削除
       Future.delayed(Duration(seconds: 30), () {
         _cache.remove(sheet.id);
         _deleteTime.remove(sheet.id);
       });
     }
     
     ExpenseSheet? restore(String id) {
       final sheet = _cache[id];
       _cache.remove(id);
       _deleteTime.remove(id);
       return sheet;
     }
   }
   ```

2. **アーカイブ機能**
   - 削除ではなく「アーカイブ」状態に変更
   - アーカイブ一覧から復元可能

### v1.3で実装予定

1. **バッチ削除**
   ```dart
   // 複数選択モード
   bool isSelectionMode = false;
   Set<String> selectedIds = {};
   
   // 一括削除ボタン
   if (isSelectionMode) {
     FloatingActionButton(
       onPressed: () => deleteMultiple(selectedIds),
       child: Icon(Icons.delete),
     );
   }
   ```

2. **削除前のエクスポート**
   - 削除前にJSON/CSVとしてエクスポート
   - 安全性の向上

## 🐛 トラブルシューティング

### 問題1: スワイプしても反応しない

**原因**: カードのInkWellとDismissibleのジェスチャーが競合

**解決方法**:
```dart
// InkWellのonTapよりもDismissibleが優先される設計
// スワイプは正常に動作する
```

### 問題2: 削除後にエラー画面が表示される

**原因**: providerの状態が正しく更新されていない

**解決方法**:
```dart
// expenseSheetListProviderのdeleteSheetメソッド内で
// ref.invalidateSelf() を呼び出す
ref.invalidateSelf();
await future;
```

### 問題3: 削除確認ダイアログが2回表示される

**原因**: 誤ってconfirmDismissを二重に呼び出している

**解決方法**:
```dart
// confirmDismissは1回だけ設定
confirmDismiss: (direction) async {
  return await _confirmDeleteSheet(context, sheet);
},
// onDismissedでは再度確認しない
```

### 問題4: 削除後もUIに残る

**原因**: エラーが発生してproviderが更新されていない

**解決方法**:
```dart
// エラーハンドリングを確認
try {
  await ref.read(expenseSheetListProvider.notifier).deleteSheet(sheet.id);
} catch (e) {
  print('削除エラー: $e');
  // エラーをユーザーに通知
}
```

## 📝 ベストプラクティス

### 1. 確認ダイアログは詳細に

```dart
// ❌ 悪い例: 最小限の情報
AlertDialog(
  title: Text('削除しますか？'),
  actions: [
    TextButton(child: Text('はい')),
    TextButton(child: Text('いいえ')),
  ],
)

// ✅ 良い例: 詳細な情報と警告
AlertDialog(
  title: Text('精算書を削除'),
  content: Column(
    children: [
      Text('削除すると、すべての明細データも失われます。'),
      // 精算書の詳細情報を表示
    ],
  ),
  actions: [
    TextButton(child: Text('キャンセル')),
    FilledButton(
      style: FilledButton.styleFrom(backgroundColor: Colors.red),
      child: Text('削除'),
    ),
  ],
)
```

### 2. エラーハンドリングは必須

```dart
// ✅ 必ずtry-catchで囲む
try {
  await deleteSheet(sheet.id);
  _showSuccessSnackBar();
} catch (e) {
  _showErrorSnackBar(e);
}
```

### 3. mounted チェック

```dart
// ✅ 非同期処理後は必ずチェック
if (context.mounted) {
  ScaffoldMessenger.of(context).showSnackBar(...);
}
```

### 4. ユニークキーの使用

```dart
// ✅ 精算書のIDをキーにする
key: Key(sheet.id),

// ❌ インデックスは使わない
key: Key('$index'),
```

## 📚 参考リソース

- [Flutter Dismissible](https://api.flutter.dev/flutter/widgets/Dismissible-class.html)
- [Material Design - Swipe](https://material.io/components/lists#behavior)
- [Riverpod - State Management](https://riverpod.dev)

---

**最終更新**: 2024年12月 - v1.1.0

**実装ステータス**: ✅ 完全実装済み